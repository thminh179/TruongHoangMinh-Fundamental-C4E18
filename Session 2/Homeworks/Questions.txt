1. Whats is Boolean? Write down 3 different expressions that result a Boolean type (ie 5 == 6)
 - Boolean is a true/false value.
 - Example expressions:
    >>> 10 > 20
    False
    >>> 2 != 3 
    True
    >>> 10 <= 10
    True

2. What is a flow chart? 
 - flow chart is a diagram that represents a workflow.

3. What is a nested conditional?
 - a nested conditional is a conditional that is contained (nested) in another conditional.

 - for example:
 if bmi > 25:
    if bmi > 30:
      print ("obese")
    else:
      print ("Overweight")
